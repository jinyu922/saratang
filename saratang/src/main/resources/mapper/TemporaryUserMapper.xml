<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.swyp.saratang.mapper.TemporaryUserMapper">

    <!-- 임시 사용자 저장 -->
    <insert id="saveTemporaryUser">
        INSERT INTO temporary_users (nickname, email, social_id, auth_provider, email_verified, regdate)
        VALUES (#{nickname}, #{email}, #{socialId}, #{authProvider}, #{emailVerified}, NOW());
    </insert>

    <select id="countByTempSocialId" resultType="int">
    SELECT COUNT(*) FROM temporary_users WHERE social_id = #{socialId}
    </select>


    <!-- 임시 사용자 삭제 (가입 완료 후) -->
    <delete id="deleteBySocialId">
        DELETE FROM temporary_users WHERE social_id = #{socialId}
    </delete>

</mapper>